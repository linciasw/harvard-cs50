# Rabbit-Hole Learning

Start with one question and follow your curiosity.

## Other Rabbit Holes

* Why are strings immutable?
* The hierarchy of exceptions
* How does exceptions work under the hood
* Why can tuples be dictionary keys?
* Why does `list.append()` return `None`?
* Why does `range()` work the way it does?
* Why are dictionaries so fast?
* Why are tuples hashable but lists aren't?
* What's actually happening when Python runs `print()`?
* What's the difference between `==` and `is`?
* Why does `0.1 + 0.2` produce a weird result?
* What's actually inside a Python object?
* What happens when a variable is assigned?
* How does Python know what type something is?
* What's the difference between an iterable and an iterator?
* What actually happens in a `for` loop?
* Why does `while True` work?
* How does `try/except` actually work?
* What's a namespace?
* What's the difference between a module, package, and library?
* What does `import` actually do?
* Research VSC in depth 

## Rule

You don't have to finish the rabbit hole.

**The point is curiosity.**

